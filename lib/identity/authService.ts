import type { Session, User } from './types'
import {
  authMode,
  getSession,
  login,
  loginDemo,
  logout,
  refreshSession,
  register,
  subscribeSession,
} from '@/lib/auth'

export {
  authMode,
  getSession,
  login,
  loginDemo,
  logout,
  refreshSession,
  register,
  subscribeSession,
}

export type { Session, User }
