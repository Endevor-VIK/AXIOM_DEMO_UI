// AXIOM_DEMO_UI — LEGACY STUB
// Canvas: C35 — assets/js/app.js
// Purpose: Deprecated legacy demo script placeholder. Not used by WEB CORE (Vite + React).
// This file remains only to keep historical structure until full removal.

(function(){
    const msg = [
      'AXIOM_DEMO_UI: legacy demo script is deprecated.',
      'The new entrypoint is /app/main.tsx (Vite + React + TS).',
      'If you see this in console, ensure index.html does NOT include /assets/js/app.js',
    ].join('\n');
    if (typeof console !== 'undefined') console.warn(msg);
  })();
  